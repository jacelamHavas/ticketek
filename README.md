# Havas CRO Projects

### HTML > JS

1. Minify static HTML [here](https://www.willpeavy.com/minifier/)
2. Convert html to JS [here](http://pojo.sodhanalibrary.com/string.html)
3. Use `$('.sectionTitle').parent().replaceWith(html);` to replace the element you want